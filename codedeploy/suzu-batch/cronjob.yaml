apiVersion: batch/v1
kind: CronJob
metadata:
  name: suzu-batch
spec:
  schedule: "5 * * * *"
  jobTemplate:
    spec:
      template:
        spec:
          containers:
          - name: suzu-api
            image: AWS_ECR_REGISTRY/suzu-api-repository:IMAGE_TAG
            envFrom:
              - configMapRef:
                  name: env-config
          restartPolicy: OnFailure
